# AI 协作记录（AI Log）

---

## 第 1 条：训练进度可视化——监控方案的设计取舍

| 字段 | 内容 |
|------|------|
| **目的** | 训练脚本已开始运行，但没有实时进度界面，希望能在浏览器中直观看到训练进度和生成效果的变化 |
| **输入** | 给 AI 看了 `train_dcgan.py` 的代码结构——脚本每轮自动保存 `generated_epoch{N}.png` 和 `loss_curve_epoch{N}.png` 到 `C:\dcgan_result`，共 50 个 epoch |
| **建议** | AI 提出两种方案：(1) 创建一个 HTML 页面，通过 HTTP 服务器自动刷新图片；(2) 直接打开已有的生成图片查看。推荐方案一 |
| **人工判断** | **采纳了方案一（HTTP 监控服务器），但做了两个改动。** 第一，将 AI 原本用 JavaScript 定时刷新的方案改为 `<meta http-equiv="refresh">`，因为训练是 CPU 密集型，页面在后台打开即可，不需要 JavaScript 动态加载增加复杂度。第二，要求 AI 把 `total_epochs` 做成参数化（`--epochs`），因为后续如果调整训练轮数，不需要改代码。此外，我还要求 AI 把脚本整理到 `code/` 目录下并加上 argparse 参数支持，而不是放在根目录作为一次性脚本 |
| **验证** | 启动监控服务器后，在浏览器访问 `http://localhost:8765`，页面正确显示进度条和最新图片。训练进行中时页面每 8 秒自动刷新一次，进度条随 epoch 推进实时更新。训练结束后页面显示"训练完成"状态 |

---

## 第 2 条：训练中断后的数据保留决策——用已有结果继续

| 字段 | 内容 |
|------|------|
| **目的** | 训练在第 41 轮后崩溃，需要决定是重新训练还是使用已有的 40 轮结果 |
| **输入** | 告知 AI 训练进程（PID 82104）已不存在，最后输出是 Epoch 41，`generator.ckpt` 和 `discriminator.ckpt` 已保存 |
| **建议** | AI 提出两个选项：(1) 从头重新训练；(2) 修改脚本加载 checkpoint 从第 42 轮断点续训。并主动说明选项二可以省去前 41 轮的时间 |
| **人工判断** | **两个选项都没有采纳，选择了第三种方案：直接以 40 轮结果作为最终输出。** 理由是：(1) DCGAN 训练40轮已经能看到明显的生成效果提升，再训练10轮对最终展示效果影响有限；(2) 第41轮的输出文件需要删除，因为它不完整（模型已保存但没有对应的生成图片结果）；(3) 要求 AI 将代码中的 `num_epochs` 从 50 修改为 40，使代码和实际结果一致。这样做的好处是：项目文档与实际训练结果完全吻合，不会出现"代码写50轮但实际只有40轮"的不一致 |
| **验证** | 确认 `result/` 目录下 epoch41 的文件已删除，`train_dcgan.py` 和 `monitor.py` 中的 epoch 数量已更新为 40，代码逻辑与最终结果一致 |

---

## 第 3 条：监控脚本的封装——从一次性代码到可复用工具

| 字段 | 内容 |
|------|------|
| **目的** | 根目录的 `monitor.py` 是一次生成的，需要整理成 `code/` 目录下的可复用版本 |
| **输入** | 给 AI 看了原始 `monitor.py`（硬编码 `RESULT_DIR = r'C:\dcgan_result'`，端口8765，50轮），以及 `train_dcgan.py` 的参数设置 |
| **建议** | AI 建议：(1) 加入 argparse 支持命令行参数；(2) 将端口、结果目录、epoch 数量都参数化；(3) 加入目录存在性检查和错误提示；(4) 添加中文文档字符串说明用法 |
| **人工判断** | **全部采纳。** 但补充了一个细节：要求 AI 在页面上区分"训练进行中"和"训练完成"两种状态——当 `current_epoch >= total_epochs` 时，页面标题和状态栏应显示"训练完成"，状态指示点从绿色变为蓝色。这个改动虽然小，但在实际使用中很关键：打开页面就能一眼看出训练是否结束，不用再数文件数量 |
| **验证** | 修改后的 `code/monitor.py` 支持 `--dir`、`--port`、`--epochs` 三个参数。运行 `python code/monitor.py --epochs 40` 后页面正确显示"Epoch: 40/40 (训练完成)"。当结果目录不存在时会提示错误信息而不是静默失败 |
